<form method="post" action="{{ path('app_boletin_delete', {'id': boletin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boletin.id) }}">
    <button class="btn">Eliminar</button>
</form>
