<form method="post" action="{{ path('app_sitios_delete', {'id': sitio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sitio.id) }}">
    <button class="btn">Delete</button>
</form>
