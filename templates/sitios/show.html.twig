{% extends 'base.html.twig' %}

{% block title %}Sitios{% endblock %}

{% block body %}
    <h1>Sitios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sitio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sitio.nombre }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ sitio.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sitios_index') }}">back to list</a>

    <a href="{{ path('app_sitios_edit', {'id': sitio.id}) }}">edit</a>

    {{ include('sitios/_delete_form.html.twig') }}
{% endblock %}
